# ============================================
# DEVELOPMENT CONFIGURATION
# Copy this file to .env.local for development
# ============================================

# Database Connection (PostgreSQL)
DATABASE_URL="postgresql://username:password@localhost:5432/serayu?schema=public"

# JWT Authentication
JWT_SECRET="your-secure-jwt-secret-key-min-32-chars"

# Public Variables
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email (Gmail SMTP) - untuk verifikasi user dan reset password
# Langkah setup Gmail:
# 1. Aktifkan 2FA di akun Google
# 2. Buat App Password di https://myaccount.google.com/apppasswords
# 3. Gunakan password 16 karakter yang dihasilkan (tanpa spasi)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="xxxx xxxx xxxx xxxx"
EMAIL_FROM="Nama Aplikasi <your-email@gmail.com>"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# NextAuth.js (V5)
AUTH_SECRET="your-auth-secret-min-32-chars"
NEXTAUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST="true"

# Cloudflare R2 Storage (S3-compatible)
# Get credentials from Cloudflare Dashboard > R2 > Manage R2 API Tokens
R2_ACCOUNT_ID="your-cloudflare-account-id"
R2_ACCESS_KEY_ID="your-r2-access-key-id"
R2_SECRET_ACCESS_KEY="your-r2-secret-access-key"
R2_BUCKET_NAME="your-bucket-name"
R2_PUBLIC_URL="https://pub-xxxxx.r2.dev"

# ============================================
# R2 CORS CONFIGURATION (Required!)
# ============================================
# Go to: Cloudflare Dashboard > R2 > your-bucket > Settings > CORS Policy
#
# For DEVELOPMENT only:
# [
#   {
#     "AllowedOrigins": ["http://localhost:3000"],
#     "AllowedMethods": ["GET", "HEAD"],
#     "AllowedHeaders": ["*"],
#     "MaxAgeSeconds": 86400
#   }
# ]
#
# For PRODUCTION (replace with your domain):
# [
#   {
#     "AllowedOrigins": ["https://yourdomain.com"],
#     "AllowedMethods": ["GET", "HEAD"],
#     "AllowedHeaders": ["*"],
#     "MaxAgeSeconds": 86400
#   }
# ]
#
# For BOTH development + production:
# [
#   {
#     "AllowedOrigins": ["http://localhost:3000", "https://yourdomain.com"],
#     "AllowedMethods": ["GET", "HEAD"],
#     "AllowedHeaders": ["*"],
#     "MaxAgeSeconds": 86400
#   }
# ]

# ============================================
# PRODUCTION NOTES
# ============================================
# For Vercel deployment, set these in Vercel Dashboard > Settings > Environment Variables:
# - DATABASE_URL: Your production PostgreSQL connection string
# - JWT_SECRET: A secure random string (32+ chars)
# - AUTH_SECRET: A secure random string (32+ chars)
# - NEXT_PUBLIC_SITE_URL: https://yourdomain.com
# - NEXT_PUBLIC_APP_URL: https://yourdomain.com
# - NEXTAUTH_URL: (not needed on Vercel, auto-detected)
# - All R2_* variables with your Cloudflare R2 credentials
# - All SMTP_* variables for email functionality
# - All Google OAuth variables
